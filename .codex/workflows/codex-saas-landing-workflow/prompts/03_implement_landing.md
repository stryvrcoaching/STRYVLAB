# Prompt 03 — Implement landing page

Use:
- `landing-workflow-orchestrator`
- `saas-landing-structure`
- `ui-ux-pro-max`
- `landing-visual-ui-motion`
- `codex-ui-execution-qa`

Use `AGENTS.md` and `LANDING_BRIEF.md` as sources of truth.

Implement or refactor the SaaS landing page.

Requirements:
- follow the canonical SaaS landing structure;
- make the hero clear and product-first;
- use outcome-driven copy;
- include proof only if real or honestly framed;
- create a premium, non-generic UI;
- add motion only where useful;
- use Framer Motion as the default React motion implementation and respect reduced motion;
- use the `liquid-glass` skill only if the approved brief explicitly selects it; keep it to one or two small surfaces, include the fallback, and do not use it on product screenshots or forms;
- support reduced motion;
- make it responsive;
- optimize media;
- preserve project conventions;
- avoid unnecessary dependencies.

After implementation:
- run available checks;
- visually inspect desktop, tablet, and mobile;
- fix visible issues.

Final output:
1. Files changed
2. Sections implemented
3. Design decisions
4. Commands run
5. Visual QA performed
6. Remaining risks/TODOs
7. UI/UX Pro Max, motion, and material decisions
