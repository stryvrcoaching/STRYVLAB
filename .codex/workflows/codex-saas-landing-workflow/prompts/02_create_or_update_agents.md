# Prompt 02 — Create or update AGENTS.md

Use the `landing-discovery-brief`, `ui-ux-pro-max`, and `landing-visual-ui-motion` skills.

Based on the repository audit and my answers, create or update `AGENTS.md`.

The file must define:
- product identity;
- positioning;
- visual direction;
- typography;
- color system;
- spacing;
- layout rules;
- component rules;
- motion rules;
- product visual rules;
- proof rules;
- accessibility;
- responsive behavior;
- performance constraints;
- do/don't list.

Do not implement the landing page yet.
Do not select Liquid Glass unless the approved visual direction explicitly asks for it. If it is selected, document the intended surfaces, browser fallback, and performance boundary in `AGENTS.md`.

Output:
1. `AGENTS.md` created/updated
2. Summary of the design direction
3. Assumptions
4. Questions that still block implementation, if any
