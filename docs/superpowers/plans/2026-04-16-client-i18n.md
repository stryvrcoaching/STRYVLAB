# Client App i18n — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fully translate the client app (FR/EN/ES) so that every page and component respects the language saved in `client_preferences.language`.

**Architecture:** A single `lib/i18n/clientTranslations.ts` file holds all FR/EN/ES strings for the client app. A `ClientI18nProvider` wraps the client layout, fetches the stored language preference on mount, and exposes a `useClientT()` hook. Every client page/component that has user-visible text consumes this hook. The existing `app/lib/i18n/translations.ts` (used by lab tools) is left untouched.

**Tech Stack:** Next.js 14 App Router, React Context, TypeScript strict, Supabase (language stored in `client_preferences.language`), Tailwind CSS DS v2.0.

---

## File Structure

| Action | File | Role |
|--------|------|------|
| **Create** | `lib/i18n/clientTranslations.ts` | All FR/EN/ES strings for client app — single source of truth |
| **Create** | `components/client/ClientI18nProvider.tsx` | `'use client'` — loads lang from `/api/client/preferences` on mount, provides `useClientT()` hook |
| **Modify** | `app/client/layout.tsx` | Wrap children with `<ClientI18nProvider>` |
| **Modify** | `components/client/BottomNav.tsx` | Use `useClientT()` for nav labels |
| **Modify** | `components/client/ContextualGreeting.tsx` | Use `useClientT()` for greeting strings |
| **Modify** | `app/client/page.tsx` | Pass lang to server-rendered strings (date locale, static text) via `ClientI18nProvider` on client side |
| **Modify** | `app/client/programme/page.tsx` | Translate hardcoded FR labels |
| **Modify** | `app/client/bilans/page.tsx` | Translate hardcoded FR strings |
| **Modify** | `app/client/progress/ProgressClientPage.tsx` | Translate period labels, insight strings, KPI labels |
| **Modify** | `app/client/progress/ProgressHeatmap.tsx` | Translate month/day labels |
| **Modify** | `app/client/progress/PRsPodium.tsx` | Translate "PR" labels |
| **Modify** | `app/client/profil/page.tsx` | Translate section titles, status badges |
| **Modify** | `app/client/profil/LogoutButton.tsx` | Translate logout modal strings |
| **Modify** | `components/client/profile/PreferencesForm.tsx` | Translate labels + save button + after-save lang switch (reload) |
| **Modify** | `app/client/programme/session/[sessionId]/SessionLogger.tsx` | Translate all UI strings |
| **Modify** | `app/client/programme/recap/[sessionLogId]/page.tsx` | Translate recap strings |

---

## Task 1: Create the translations dictionary

**Files:**
- Create: `lib/i18n/clientTranslations.ts`

- [ ] **Step 1: Create the file with all FR/EN/ES strings**

```typescript
// lib/i18n/clientTranslations.ts

export type ClientLang = 'fr' | 'en' | 'es'

export const clientDict = {
  // ── BottomNav ──
  'nav.home':      { fr: 'Accueil',   en: 'Home',      es: 'Inicio' },
  'nav.programme': { fr: 'Programme', en: 'Program',    es: 'Programa' },
  'nav.progress':  { fr: 'Progrès',   en: 'Progress',   es: 'Progreso' },
  'nav.bilans':    { fr: 'Bilans',     en: 'Assessments',es: 'Evaluaciones' },
  'nav.profil':    { fr: 'Profil',     en: 'Profile',    es: 'Perfil' },

  // ── Home page ──
  'home.section':          { fr: 'Accueil',    en: 'Home',      es: 'Inicio' },
  'home.noProgram.title':  { fr: 'Pas encore de programme', en: 'No program yet', es: 'Aún sin programa' },
  'home.noProgram.desc':   { fr: 'Ton coach prépare ton programme personnalisé. Tu seras notifié dès qu\'il est prêt.', en: 'Your coach is preparing your personalised program. You will be notified when it is ready.', es: 'Tu coach está preparando tu programa personalizado. Te notificaremos cuando esté listo.' },
  'home.session.label':    { fr: 'Séance du jour', en: "Today's session", es: 'Sesión de hoy' },
  'home.session.start':    { fr: 'Commencer la séance', en: 'Start session', es: 'Empezar sesión' },
  'home.session.duration': { fr: 'Durée', en: 'Duration', es: 'Duración' },
  'home.session.sets':     { fr: 'Sets',   en: 'Sets',      es: 'Series' },
  'home.session.exercises':{ fr: 'Exercices', en: 'Exercises', es: 'Ejercicios' },
  'home.rest.noSession':   { fr: 'Pas de séance aujourd\'hui', en: 'No session today', es: 'Sin sesión hoy' },
  'home.rest.next':        { fr: 'Prochaine :', en: 'Next:', es: 'Siguiente:' },
  'home.rest.seeProgram':  { fr: 'Voir le programme', en: 'See program', es: 'Ver programa' },
  'home.rest.noActive':    { fr: 'Aucun programme actif', en: 'No active program', es: 'Sin programa activo' },
  'home.week.label':       { fr: 'Cette semaine', en: 'This week', es: 'Esta semana' },
  'home.coachMsg.label':   { fr: 'Message de ton coach', en: 'Message from your coach', es: 'Mensaje de tu coach' },
  'home.bilans.one':       { fr: 'Bilan en attente', en: 'Pending assessment', es: 'Evaluación pendiente' },
  'home.bilans.many':      { fr: '{n} bilans en attente', en: '{n} pending assessments', es: '{n} evaluaciones pendientes' },
  'home.bilans.cta':       { fr: 'Remplis-le pour ton coach', en: 'Fill it in for your coach', es: 'Complétala para tu coach' },

  // ── Programme page ──
  'programme.section':      { fr: 'Programme', en: 'Program',  es: 'Programa' },
  'programme.rest.today':   { fr: 'Repos aujourd\'hui', en: 'Rest today', es: 'Descanso hoy' },
  'programme.week.label':   { fr: 'Programme de la semaine', en: 'Weekly schedule', es: 'Programa semanal' },
  'programme.noProgram':    { fr: 'Aucun programme actif', en: 'No active program', es: 'Sin programa activo' },
  'programme.noProgram.desc':{ fr: 'Ton coach n\'a pas encore assigné de programme.', en: 'Your coach has not assigned a program yet.', es: 'Tu coach aún no ha asignado un programa.' },
  'programme.session.start':{ fr: 'Commencer', en: 'Start', es: 'Empezar' },
  'programme.session.duration':{ fr: 'Durée est.', en: 'Est. duration', es: 'Duración est.' },
  'programme.session.sets': { fr: 'Sets', en: 'Sets', es: 'Series' },
  'programme.session.exercises':{ fr: 'exercices', en: 'exercises', es: 'ejercicios' },
  'programme.session.rest': { fr: 'Repos moy.', en: 'Avg rest', es: 'Descanso med.' },
  'programme.session.rir':  { fr: 'RIR moy.', en: 'Avg RIR', es: 'RIR med.' },
  'programme.days.short':   { fr: ['Lun','Mar','Mer','Jeu','Ven','Sam','Dim'], en: ['Mon','Tue','Wed','Thu','Fri','Sat','Sun'], es: ['Lun','Mar','Mié','Jue','Vie','Sáb','Dom'] },
  'programme.days.full':    { fr: ['Lundi','Mardi','Mercredi','Jeudi','Vendredi','Samedi','Dimanche'], en: ['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'], es: ['Lunes','Martes','Miércoles','Jueves','Viernes','Sábado','Domingo'] },

  // ── SessionLogger ──
  'logger.section':          { fr: 'Programme', en: 'Program', es: 'Programa' },
  'logger.set':              { fr: 'Série', en: 'Set', es: 'Serie' },
  'logger.target.label':     { fr: 'Prévu', en: 'Target', es: 'Objetivo' },
  'logger.actual.label':     { fr: 'Réalisé', en: 'Actual', es: 'Realizado' },
  'logger.weight.kg':        { fr: 'kg', en: 'kg', es: 'kg' },
  'logger.reps':             { fr: 'reps', en: 'reps', es: 'reps' },
  'logger.rir.label':        { fr: 'RIR ressenti', en: 'Actual RIR', es: 'RIR real' },
  'logger.rir.target':       { fr: 'RIR cible', en: 'Target RIR', es: 'RIR objetivo' },
  'logger.side.left':        { fr: 'Gauche', en: 'Left', es: 'Izquierda' },
  'logger.side.right':       { fr: 'Droite', en: 'Right', es: 'Derecha' },
  'logger.rest.title':       { fr: 'Repos', en: 'Rest', es: 'Descanso' },
  'logger.rest.skip':        { fr: 'Passer le repos', en: 'Skip rest', es: 'Saltar descanso' },
  'logger.rest.overtime':    { fr: 'Dépassement', en: 'Overtime', es: 'Sobrepasado' },
  'logger.note.placeholder': { fr: 'Note de ressenti…', en: 'Session note…', es: 'Nota de sesión…' },
  'logger.finish':           { fr: 'Terminer la séance', en: 'Finish session', es: 'Finalizar sesión' },
  'logger.finish.confirm':   { fr: 'Terminer ?', en: 'Finish?', es: '¿Finalizar?' },
  'logger.finish.incomplete':{ fr: 'sets non complétés', en: 'sets not completed', es: 'series sin completar' },
  'logger.finish.action':    { fr: 'Terminer quand même', en: 'Finish anyway', es: 'Finalizar de todos modos' },
  'logger.finish.cancel':    { fr: 'Continuer', en: 'Continue', es: 'Continuar' },
  'logger.demo.show':        { fr: 'Voir la démo', en: 'View demo', es: 'Ver demostración' },
  'logger.demo.hide':        { fr: 'Réduire', en: 'Collapse', es: 'Reducir' },
  'logger.prev':             { fr: 'Préc.', en: 'Prev', es: 'Anterior' },
  'logger.next':             { fr: 'Suiv.', en: 'Next', es: 'Siguiente' },
  'logger.lastPerf':         { fr: 'Dernière perf', en: 'Last perf', es: 'Último rend.' },

  // ── Recap page ──
  'recap.section':           { fr: 'Programme', en: 'Program', es: 'Programa' },
  'recap.title':             { fr: 'Récap séance', en: 'Session recap', es: 'Resumen sesión' },
  'recap.duration':          { fr: 'Durée', en: 'Duration', es: 'Duración' },
  'recap.sets':              { fr: 'Sets', en: 'Sets', es: 'Series' },
  'recap.volume':            { fr: 'Volume', en: 'Volume', es: 'Volumen' },
  'recap.exercises':         { fr: 'Exercices', en: 'Exercises', es: 'Ejercicios' },
  'recap.perEx.weight':      { fr: 'Charge', en: 'Weight', es: 'Carga' },
  'recap.perEx.reps':        { fr: 'Reps', en: 'Reps', es: 'Reps' },
  'recap.perEx.rir':         { fr: 'RIR', en: 'RIR', es: 'RIR' },
  'recap.backHome':          { fr: 'Retour accueil', en: 'Back to home', es: 'Volver al inicio' },

  // ── Bilans page ──
  'bilans.section':          { fr: 'Suivi', en: 'Tracking', es: 'Seguimiento' },
  'bilans.title':            { fr: 'Mes bilans', en: 'My assessments', es: 'Mis evaluaciones' },
  'bilans.empty.title':      { fr: 'Aucun bilan pour le moment', en: 'No assessments yet', es: 'Sin evaluaciones aún' },
  'bilans.empty.desc':       { fr: 'Ton coach t\'enverra un bilan à remplir prochainement.', en: 'Your coach will send you an assessment to fill in soon.', es: 'Tu coach te enviará pronto una evaluación.' },
  'bilans.todo.section':     { fr: 'À remplir', en: 'To fill in', es: 'Para completar' },
  'bilans.todo.sentOn':      { fr: 'Envoyé le', en: 'Sent on', es: 'Enviado el' },
  'bilans.todo.cta':         { fr: 'Remplir le bilan', en: 'Fill in assessment', es: 'Completar evaluación' },
  'bilans.history.section':  { fr: 'Historique', en: 'History', es: 'Historial' },
  'bilans.status.pending':   { fr: 'En attente', en: 'Pending', es: 'Pendiente' },
  'bilans.status.in_progress':{ fr: 'En cours', en: 'In progress', es: 'En curso' },
  'bilans.status.completed': { fr: 'Complété', en: 'Completed', es: 'Completado' },
  'bilans.status.expired':   { fr: 'Expiré', en: 'Expired', es: 'Expirado' },
  'bilans.count':            { fr: '{n} bilan{pl}', en: '{n} assessment{pl}', es: '{n} evaluación{pl}' },

  // ── Progress page ──
  'progress.section':        { fr: 'Progression', en: 'Progress', es: 'Progreso' },
  'progress.streak.title':   { fr: 'Streak', en: 'Streak', es: 'Racha' },
  'progress.streak.days':    { fr: '{n} jour{pl}', en: '{n} day{pl}', es: '{n} día{pl}' },
  'progress.streak.record':  { fr: 'Record', en: 'Record', es: 'Récord' },
  'progress.streak.zero':    { fr: 'Lance-toi !', en: 'Get started!', es: '¡Empieza!' },
  'progress.streak.fire':    { fr: 'En feu 🔥', en: 'On fire 🔥', es: '¡En racha! 🔥' },
  'progress.period.7':       { fr: '7j',  en: '7d',  es: '7d' },
  'progress.period.30':      { fr: '30j', en: '30d', es: '30d' },
  'progress.period.90':      { fr: '90j', en: '90d', es: '90d' },
  'progress.period.all':     { fr: 'Tout', en: 'All', es: 'Todo' },
  'progress.kpi.sessions':   { fr: 'Séances', en: 'Sessions', es: 'Sesiones' },
  'progress.kpi.volume':     { fr: 'Volume', en: 'Volume', es: 'Volumen' },
  'progress.kpi.sets':       { fr: 'Sets', en: 'Sets', es: 'Series' },
  'progress.kpi.avgDuration':{ fr: 'Durée moy.', en: 'Avg duration', es: 'Duración med.' },
  'progress.history.title':  { fr: 'Historique séances', en: 'Session history', es: 'Historial sesiones' },
  'progress.history.setsLabel':{ fr: 'sets', en: 'sets', es: 'series' },
  'progress.history.noSessions':{ fr: 'Aucune séance sur cette période', en: 'No sessions in this period', es: 'Sin sesiones en este período' },
  'progress.prs.title':      { fr: 'Records personnels', en: 'Personal records', es: 'Récords personales' },
  'progress.heatmap.months': {
    fr: ['Jan','Fév','Mar','Avr','Mai','Jun','Jul','Aoû','Sep','Oct','Nov','Déc'],
    en: ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'],
    es: ['Ene','Feb','Mar','Abr','May','Jun','Jul','Ago','Sep','Oct','Nov','Dic'],
  },
  'progress.heatmap.days':   {
    fr: ['L','M','M','J','V','S','D'],
    en: ['M','T','W','T','F','S','S'],
    es: ['L','M','X','J','V','S','D'],
  },

  // ── Profil page ──
  'profil.section':          { fr: 'Compte', en: 'Account', es: 'Cuenta' },
  'profil.title':            { fr: 'Mon profil', en: 'My profile', es: 'Mi perfil' },
  'profil.status.active':    { fr: 'Actif', en: 'Active', es: 'Activo' },
  'profil.section.info':     { fr: 'Informations personnelles', en: 'Personal information', es: 'Información personal' },
  'profil.section.notif':    { fr: 'Notifications', en: 'Notifications', es: 'Notificaciones' },
  'profil.section.prefs':    { fr: 'Préférences d\'affichage', en: 'Display preferences', es: 'Preferencias de visualización' },
  'profil.section.security': { fr: 'Sécurité', en: 'Security', es: 'Seguridad' },
  'profil.memberSince':      { fr: 'Membre depuis', en: 'Member since', es: 'Miembro desde' },
  'profil.logout':           { fr: 'Se déconnecter', en: 'Log out', es: 'Cerrar sesión' },
  'profil.logout.confirm.title':{ fr: 'Se déconnecter ?', en: 'Log out?', es: '¿Cerrar sesión?' },
  'profil.logout.confirm.desc': { fr: 'Tu seras redirigé vers la page de connexion.', en: 'You will be redirected to the login page.', es: 'Serás redirigido a la página de inicio de sesión.' },
  'profil.logout.cancel':    { fr: 'Annuler', en: 'Cancel', es: 'Cancelar' },
  'profil.logout.action':    { fr: 'Déconnecter', en: 'Log out', es: 'Cerrar sesión' },
  'profil.logout.loading':   { fr: 'Déconnexion…', en: 'Logging out…', es: 'Cerrando sesión…' },

  // ── Preferences form ──
  'prefs.weight':            { fr: 'Unité de poids', en: 'Weight unit', es: 'Unidad de peso' },
  'prefs.height':            { fr: 'Unité de taille', en: 'Height unit', es: 'Unidad de altura' },
  'prefs.language':          { fr: 'Langue', en: 'Language', es: 'Idioma' },
  'prefs.save':              { fr: 'Sauvegarder', en: 'Save', es: 'Guardar' },
  'prefs.saving':            { fr: 'Sauvegarde…', en: 'Saving…', es: 'Guardando…' },
  'prefs.saved':             { fr: 'Sauvegardé', en: 'Saved', es: 'Guardado' },

  // ── Greeting ──
  'greeting.session.morning':   { fr: 'C\'est jour de séance, {name} 💪', en: 'It\'s session day, {name} 💪', es: 'Es día de sesión, {name} 💪' },
  'greeting.session.afternoon': { fr: 'Ta séance t\'attend, {name} 💪', en: 'Your session is waiting, {name} 💪', es: 'Tu sesión te espera, {name} 💪' },
  'greeting.session.evening':   { fr: 'Bonne séance ce soir, {name} 💪', en: 'Great session tonight, {name} 💪', es: 'Buena sesión esta noche, {name} 💪' },
  'greeting.rest.morning':      { fr: 'Bonjour {name} — jour de récup aujourd\'hui', en: 'Good morning {name} — recovery day today', es: 'Buenos días {name} — día de recuperación' },
  'greeting.rest.afternoon':    { fr: 'Bonne après-midi {name} — profite de ta récup', en: 'Good afternoon {name} — enjoy your rest', es: 'Buenas tardes {name} — disfruta tu descanso' },
  'greeting.rest.evening':      { fr: 'Bonne soirée {name} — repose-toi bien', en: 'Good evening {name} — rest well', es: 'Buenas noches {name} — descansa bien' },

  // ── Common ──
  'common.loading':   { fr: 'Chargement…', en: 'Loading…', es: 'Cargando…' },
  'common.error':     { fr: 'Erreur', en: 'Error', es: 'Error' },
  'common.cancel':    { fr: 'Annuler', en: 'Cancel', es: 'Cancelar' },
  'common.confirm':   { fr: 'Confirmer', en: 'Confirm', es: 'Confirmar' },
  'common.min':       { fr: 'min', en: 'min', es: 'min' },
  'common.sec':       { fr: 's', en: 's', es: 's' },
  'common.kg':        { fr: 'kg', en: 'kg', es: 'kg' },
} satisfies Record<string, { fr: string | string[]; en: string | string[]; es: string | string[] }>

export type ClientDictKey = keyof typeof clientDict

export function ct(lang: ClientLang, key: ClientDictKey, vars?: Record<string, string | number>): string {
  const entry = clientDict[key]
  const raw = entry[lang] as string
  if (!vars) return raw
  return raw.replace(/\{(\w+)\}/g, (_, k) => String(vars[k] ?? ''))
}

/** Plural helper: replaces {pl} with 's' (EN) or '' (FR/ES) if n !== 1 */
export function ctp(lang: ClientLang, key: ClientDictKey, n: number): string {
  const raw = ct(lang, key, { n })
  const pl = lang === 'en' ? (n !== 1 ? 's' : '') : ''
  return raw.replace('{pl}', pl)
}

/** Returns the string array for array-valued keys (days, months) */
export function cta(lang: ClientLang, key: ClientDictKey): string[] {
  const entry = clientDict[key]
  return entry[lang] as string[]
}
```

- [ ] **Step 2: TypeScript check**

```bash
npx tsc --noEmit 2>&1 | grep "clientTranslations"
```
Expected: no output (0 errors).

- [ ] **Step 3: Commit**

```bash
git add lib/i18n/clientTranslations.ts
git commit -m "feat(i18n): client translations dictionary — FR/EN/ES all client pages"
```

---

## Task 2: Create ClientI18nProvider

**Files:**
- Create: `components/client/ClientI18nProvider.tsx`

- [ ] **Step 1: Create the provider**

```typescript
// components/client/ClientI18nProvider.tsx
'use client'

import React, { createContext, useContext, useState, useEffect } from 'react'
import { type ClientLang, ct, ctp, cta, type ClientDictKey } from '@/lib/i18n/clientTranslations'

interface ClientI18nContextType {
  lang: ClientLang
  t: (key: ClientDictKey, vars?: Record<string, string | number>) => string
  tp: (key: ClientDictKey, n: number) => string
  ta: (key: ClientDictKey) => string[]
}

const ClientI18nContext = createContext<ClientI18nContextType>({
  lang: 'fr',
  t: (key, vars) => ct('fr', key, vars),
  tp: (key, n) => ctp('fr', key, n),
  ta: (key) => cta('fr', key),
})

export function ClientI18nProvider({ children }: { children: React.ReactNode }) {
  const [lang, setLang] = useState<ClientLang>('fr')

  useEffect(() => {
    // Try localStorage first (instant, no flash)
    const stored = localStorage.getItem('client_lang') as ClientLang | null
    if (stored && ['fr', 'en', 'es'].includes(stored)) {
      setLang(stored)
    }
    // Then sync from DB (authoritative)
    fetch('/api/client/preferences')
      .then(r => r.ok ? r.json() : null)
      .then(data => {
        const dbLang = data?.preferences?.language as ClientLang | undefined
        if (dbLang && ['fr', 'en', 'es'].includes(dbLang)) {
          setLang(dbLang)
          localStorage.setItem('client_lang', dbLang)
        }
      })
      .catch(() => {/* silent — keep stored/default */})
  }, [])

  const value: ClientI18nContextType = {
    lang,
    t: (key, vars) => ct(lang, key, vars),
    tp: (key, n) => ctp(lang, key, n),
    ta: (key) => cta(lang, key),
  }

  return (
    <ClientI18nContext.Provider value={value}>
      {children}
    </ClientI18nContext.Provider>
  )
}

export function useClientT() {
  return useContext(ClientI18nContext)
}
```

- [ ] **Step 2: TypeScript check**

```bash
npx tsc --noEmit 2>&1 | grep "ClientI18nProvider"
```
Expected: no output.

- [ ] **Step 3: Commit**

```bash
git add components/client/ClientI18nProvider.tsx
git commit -m "feat(i18n): ClientI18nProvider — loads lang from DB/localStorage, exposes useClientT()"
```

---

## Task 3: Wire provider into client layout + update PreferencesForm to reload on lang change

**Files:**
- Modify: `app/client/layout.tsx`
- Modify: `components/client/profile/PreferencesForm.tsx`

- [ ] **Step 1: Update `app/client/layout.tsx`**

Replace the entire file content:

```typescript
// app/client/layout.tsx
import type { Metadata, Viewport } from 'next'
import ConditionalClientShell from '@/components/client/ConditionalClientShell'
import ServiceWorkerRegistrar from '@/components/client/ServiceWorkerRegistrar'
import { ClientI18nProvider } from '@/components/client/ClientI18nProvider'

export const metadata: Metadata = {
  manifest: '/manifest.json',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'black-translucent',
    title: 'STRYV',
  },
}

export const viewport: Viewport = {
  themeColor: '#121212',
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
}

export default function ClientLayout({ children }: { children: React.ReactNode }) {
  return (
    <ClientI18nProvider>
      <div className="min-h-screen bg-[#121212]">
        <ServiceWorkerRegistrar />
        <ConditionalClientShell>{children}</ConditionalClientShell>
      </div>
    </ClientI18nProvider>
  )
}
```

- [ ] **Step 2: Update `components/client/profile/PreferencesForm.tsx`** — add lang reload on save

Replace the `handleSave` function and add the lang cache update:

```typescript
// components/client/profile/PreferencesForm.tsx
'use client'

import { useState } from 'react'
import { Check, Loader2 } from 'lucide-react'

interface Prefs {
  weight_unit: 'kg' | 'lbs'
  height_unit: 'cm' | 'ft'
  language:    'fr' | 'en' | 'es'
}

type SaveState = 'idle' | 'saving' | 'saved' | 'error'

export default function PreferencesForm({ initial }: { initial: Prefs }) {
  const [prefs, setPrefs] = useState<Prefs>(initial)
  const [saveState, setSaveState] = useState<SaveState>('idle')

  function update<K extends keyof Prefs>(key: K, value: Prefs[K]) {
    setPrefs((p) => ({ ...p, [key]: value }))
    if (saveState !== 'idle') setSaveState('idle')
  }

  async function handleSave() {
    setSaveState('saving')
    const res = await fetch('/api/client/preferences', {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(prefs),
    })
    if (res.ok) {
      setSaveState('saved')
      // Persist lang to localStorage so ClientI18nProvider picks it up on next render
      localStorage.setItem('client_lang', prefs.language)
      // Full page reload to re-render all server components in the new language
      setTimeout(() => window.location.reload(), 800)
    } else {
      setSaveState('error')
    }
  }

  return (
    <div className="flex flex-col gap-4">
      <ToggleGroup
        label="Unité de poids"
        options={[{ value: 'kg', label: 'kg' }, { value: 'lbs', label: 'lbs' }]}
        value={prefs.weight_unit}
        onChange={(v) => update('weight_unit', v as 'kg' | 'lbs')}
      />
      <ToggleGroup
        label="Unité de taille"
        options={[{ value: 'cm', label: 'cm' }, { value: 'ft', label: 'ft / in' }]}
        value={prefs.height_unit}
        onChange={(v) => update('height_unit', v as 'cm' | 'ft')}
      />
      <ToggleGroup
        label="Langue"
        options={[
          { value: 'fr', label: '🇫🇷 Français' },
          { value: 'en', label: '🇬🇧 English' },
          { value: 'es', label: '🇪🇸 Español' },
        ]}
        value={prefs.language}
        onChange={(v) => update('language', v as 'fr' | 'en' | 'es')}
      />

      <button
        onClick={handleSave}
        disabled={saveState === 'saving'}
        className={`w-full py-2.5 rounded-xl text-sm font-bold transition-all flex items-center justify-center gap-2 ${
          saveState === 'saved'
            ? 'bg-[#1f8a65] text-white'
            : 'bg-[#1f8a65] text-white hover:bg-[#217356] disabled:opacity-50'
        }`}
      >
        {saveState === 'saving' && <Loader2 size={14} className="animate-spin" />}
        {saveState === 'saved'  && <Check size={14} />}
        {saveState === 'saved' ? 'Sauvegardé' : saveState === 'saving' ? 'Sauvegarde…' : 'Sauvegarder'}
      </button>
    </div>
  )
}

function ToggleGroup({
  label,
  options,
  value,
  onChange,
}: {
  label: string
  options: { value: string; label: string }[]
  value: string
  onChange: (v: string) => void
}) {
  return (
    <div>
      <p className="text-[10px] font-bold text-white/55 uppercase tracking-[0.18em] mb-2">{label}</p>
      <div className="flex gap-2 flex-wrap">
        {options.map((o) => (
          <button
            key={o.value}
            onClick={() => onChange(o.value)}
            className={`px-4 py-2 rounded-lg text-sm font-medium transition-all ${
              value === o.value
                ? 'bg-[#1f8a65] text-white'
                : 'bg-white/[0.04] text-white/55 hover:text-white/80'
            }`}
          >
            {o.label}
          </button>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: TypeScript check**

```bash
npx tsc --noEmit 2>&1 | grep -E "layout|PreferencesForm"
```
Expected: no output.

- [ ] **Step 4: Commit**

```bash
git add app/client/layout.tsx components/client/profile/PreferencesForm.tsx
git commit -m "feat(i18n): wire ClientI18nProvider into client layout, reload on lang save"
```

---

## Task 4: Translate BottomNav + ContextualGreeting

**Files:**
- Modify: `components/client/BottomNav.tsx`
- Modify: `components/client/ContextualGreeting.tsx`

- [ ] **Step 1: Update BottomNav**

```typescript
// components/client/BottomNav.tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { Home, ClipboardList, User, Dumbbell, TrendingUp } from 'lucide-react'
import { useClientT } from './ClientI18nProvider'

export default function BottomNav() {
  const pathname = usePathname()
  const { t } = useClientT()

  const NAV = [
    { href: '/client',           labelKey: 'nav.home'      as const, icon: Home },
    { href: '/client/programme', labelKey: 'nav.programme' as const, icon: Dumbbell },
    { href: '/client/progress',  labelKey: 'nav.progress'  as const, icon: TrendingUp },
    { href: '/client/bilans',    labelKey: 'nav.bilans'    as const, icon: ClipboardList },
    { href: '/client/profil',    labelKey: 'nav.profil'    as const, icon: User },
  ]

  return (
    <nav className="fixed bottom-0 left-0 right-0 z-50 bg-[#121212]/95 backdrop-blur-xl border-t-[0.3px] border-white/[0.06] pb-safe">
      <div className="flex max-w-lg mx-auto">
        {NAV.map(({ href, labelKey, icon: Icon }) => {
          const active = href === '/client' ? pathname === '/client' : pathname.startsWith(href)
          return (
            <Link
              key={href}
              href={href}
              className={`flex-1 flex flex-col items-center justify-center gap-1 py-3 transition-colors ${
                active ? 'text-[#1f8a65]' : 'text-white/35 hover:text-white/60'
              }`}
            >
              <Icon size={20} strokeWidth={active ? 2.2 : 1.8} />
              <span className="text-[10px] font-medium">{t(labelKey)}</span>
            </Link>
          )
        })}
      </div>
    </nav>
  )
}
```

- [ ] **Step 2: Update ContextualGreeting**

```typescript
// components/client/ContextualGreeting.tsx
'use client'

import { useEffect, useState } from 'react'
import { useClientT } from './ClientI18nProvider'

interface Props {
  firstName: string
  hasSessionToday: boolean
}

export default function ContextualGreeting({ firstName, hasSessionToday }: Props) {
  const { t } = useClientT()
  const [greeting, setGreeting] = useState('')

  useEffect(() => {
    const hour = new Date().getHours()
    let key: Parameters<typeof t>[0]
    if (hasSessionToday) {
      key = hour < 12 ? 'greeting.session.morning'
          : hour < 18 ? 'greeting.session.afternoon'
          : 'greeting.session.evening'
    } else {
      key = hour < 12 ? 'greeting.rest.morning'
          : hour < 18 ? 'greeting.rest.afternoon'
          : 'greeting.rest.evening'
    }
    setGreeting(t(key, { name: firstName }))
  }, [firstName, hasSessionToday, t])

  if (!greeting) return null

  return (
    <p className="text-[15px] font-semibold text-white leading-snug">{greeting}</p>
  )
}
```

- [ ] **Step 3: TypeScript check**

```bash
npx tsc --noEmit 2>&1 | grep -E "BottomNav|ContextualGreeting"
```
Expected: no output.

- [ ] **Step 4: Commit**

```bash
git add components/client/BottomNav.tsx components/client/ContextualGreeting.tsx
git commit -m "feat(i18n): translate BottomNav labels and ContextualGreeting strings"
```

---

## Task 5: Translate client Home page (`app/client/page.tsx`)

**Files:**
- Modify: `app/client/page.tsx`

The home page is a **Server Component** — it cannot use `useClientT()` directly. The solution: extract all client-side interactive text into a small `'use client'` wrapper component `HomeClientStrings.tsx`, and keep purely static translated strings server-side by reading lang from DB at render time.

**Approach:** Add `lang` fetch from `client_preferences` in the server component (already fetched in profil page pattern), then pass `lang` to a `HomeContent` client component that uses `ct()` directly.

- [ ] **Step 1: Update `app/client/page.tsx`**

Add the lang fetch and pass it down. Find the existing parallel fetches block and add the preferences fetch:

In `app/client/page.tsx`, after the existing imports add:
```typescript
import { ct, cta, type ClientLang } from '@/lib/i18n/clientTranslations'
```

In the server component, add a preferences fetch to the existing `Promise.all`:
```typescript
// Add as 5th item in the Promise.all:
clientId
  ? service
      .from('client_preferences')
      .select('language')
      .eq('client_id', clientId)
      .maybeSingle()
  : Promise.resolve({ data: null }),
```

Destructure it:
```typescript
const [programResult, pendingResult, logsThisWeekResult, coachNoteResult, prefsResult] = await Promise.all([...])
const lang: ClientLang = (['fr','en','es'].includes((prefsResult as any)?.data?.language) ? (prefsResult as any).data.language : 'fr') as ClientLang
const daysShort = cta(lang, 'programme.days.short')
const daysFull  = cta(lang, 'programme.days.full')
```

Replace every hardcoded French string in the JSX with `ct(lang, 'key')` calls. The full diff for key strings:

| Old FR string | New call |
|---|---|
| `'Accueil'` (section label) | `ct(lang, 'home.section')` |
| `'Pas encore de programme'` | `ct(lang, 'home.noProgram.title')` |
| `'Ton coach prépare…'` | `ct(lang, 'home.noProgram.desc')` |
| `'Séance du jour'` | `ct(lang, 'home.session.label')` |
| `'Durée'` | `ct(lang, 'home.session.duration')` |
| `'Sets'` | `ct(lang, 'home.session.sets')` |
| `'Exercices'` | `ct(lang, 'home.session.exercises')` |
| `'Commencer la séance'` | `ct(lang, 'home.session.start')` |
| `'Pas de séance aujourd\'hui'` | `ct(lang, 'home.rest.noSession')` |
| `'Prochaine :'` | `ct(lang, 'home.rest.next')` |
| `'Voir le programme'` | `ct(lang, 'home.rest.seeProgram')` |
| `'Aucun programme actif'` | `ct(lang, 'home.rest.noActive')` |
| `'Cette semaine'` | `ct(lang, 'home.week.label')` |
| `'Message de ton coach'` | `ct(lang, 'home.coachMsg.label')` |
| `DAYS_FULL` array reference | `daysFull` |
| `.toLocaleDateString('fr-FR', ...)` | `.toLocaleDateString(lang === 'fr' ? 'fr-FR' : lang === 'es' ? 'es-ES' : 'en-GB', ...)` |
| Bilan pending count string | `pendingCount === 1 ? ct(lang,'home.bilans.one') : ct(lang,'home.bilans.many',{n:pendingCount})` |
| `'Remplis-le pour ton coach'` | `ct(lang, 'home.bilans.cta')` |

Also remove the now-unused `DAYS_FR` and `DAYS_FULL` constants at the top of the file and replace `NoProgramPage` component strings.

- [ ] **Step 2: TypeScript check**

```bash
npx tsc --noEmit 2>&1 | grep "app/client/page"
```
Expected: no output.

- [ ] **Step 3: Commit**

```bash
git add app/client/page.tsx
git commit -m "feat(i18n): translate client home page — reads lang from DB"
```

---

## Task 6: Translate Programme page (`app/client/programme/page.tsx`)

**Files:**
- Modify: `app/client/programme/page.tsx`

Same pattern as Task 5 — Server Component, fetch lang from DB.

- [ ] **Step 1: Add lang fetch**

Add to imports:
```typescript
import { ct, cta, type ClientLang } from '@/lib/i18n/clientTranslations'
```

Add preferences fetch alongside program fetch (in the same Supabase client fetch block):
```typescript
const [programsResult, prefsResult] = await Promise.all([
  service.from('programs').select(`...`).eq('client_id', client.id).eq('status','active').order('created_at',{ascending:false}).limit(1),
  service.from('client_preferences').select('language').eq('client_id', client.id).maybeSingle(),
])
const programs = programsResult?.data
const lang: ClientLang = (['fr','en','es'].includes((prefsResult as any)?.data?.language) ? (prefsResult as any).data.language : 'fr') as ClientLang
const daysShort = cta(lang, 'programme.days.short')
const daysFull  = cta(lang, 'programme.days.full')
```

- [ ] **Step 2: Replace all hardcoded FR strings**

| Old string | New call |
|---|---|
| `'Programme'` (section label) | `ct(lang, 'programme.section')` |
| `'Repos aujourd\'hui'` | `ct(lang, 'programme.rest.today')` |
| `'Programme de la semaine'` | `ct(lang, 'programme.week.label')` |
| `'Aucun programme actif'` | `ct(lang, 'programme.noProgram')` |
| `'Ton coach n\'a pas encore assigné…'` | `ct(lang, 'programme.noProgram.desc')` |
| `'Commencer'` | `ct(lang, 'programme.session.start')` |
| `'Durée est.'` | `ct(lang, 'programme.session.duration')` |
| `'sets'` | `ct(lang, 'programme.session.sets')` |
| `'exercices'` | `ct(lang, 'programme.session.exercises')` |
| `'Repos moy.'` | `ct(lang, 'programme.session.rest')` |
| `'RIR moy.'` | `ct(lang, 'programme.session.rir')` |
| `DAYS_FR` / `DAYS_FULL` references | `daysShort` / `daysFull` |
| date locale `'fr-FR'` | `lang === 'fr' ? 'fr-FR' : lang === 'es' ? 'es-ES' : 'en-GB'` |

Remove unused `DAYS_FR` / `DAYS_FULL` constants.

- [ ] **Step 3: TypeScript check**

```bash
npx tsc --noEmit 2>&1 | grep "programme/page"
```
Expected: no output.

- [ ] **Step 4: Commit**

```bash
git add app/client/programme/page.tsx
git commit -m "feat(i18n): translate programme page — days/labels/CTAs in FR/EN/ES"
```

---

## Task 7: Translate Bilans page + SessionLogger + Recap page

**Files:**
- Modify: `app/client/bilans/page.tsx`
- Modify: `app/client/programme/session/[sessionId]/SessionLogger.tsx`
- Modify: `app/client/programme/recap/[sessionLogId]/page.tsx`

### Bilans page (Server Component)

- [ ] **Step 1: Add lang fetch to bilans page**

Add to imports:
```typescript
import { ct, type ClientLang } from '@/lib/i18n/clientTranslations'
```

After `resolveClientFromUser`, add:
```typescript
const prefsRes = client
  ? await service.from('client_preferences').select('language').eq('client_id', client.id).maybeSingle()
  : { data: null }
const lang: ClientLang = (['fr','en','es'].includes((prefsRes as any)?.data?.language) ? (prefsRes as any).data.language : 'fr') as ClientLang
```

Replace all hardcoded strings:

| Old | New |
|---|---|
| `'Suivi'` section label | `ct(lang, 'bilans.section')` |
| `'Mes bilans'` title | `ct(lang, 'bilans.title')` |
| `'{n} bilan{pl}'` count | use `ctp(lang, 'bilans.count', n)` — add `ctp` to imports |
| `'Aucun bilan pour le moment'` | `ct(lang, 'bilans.empty.title')` |
| `'Ton coach t\'enverra…'` | `ct(lang, 'bilans.empty.desc')` |
| `'À remplir'` section | `ct(lang, 'bilans.todo.section')` |
| `'Envoyé le'` | `ct(lang, 'bilans.todo.sentOn')` |
| `'Remplir le bilan'` | `ct(lang, 'bilans.todo.cta')` |
| `'Historique'` section | `ct(lang, 'bilans.history.section')` |
| Status map labels | use `ct(lang, 'bilans.status.pending')`, `ct(lang, 'bilans.status.in_progress')`, etc. |
| date locale `'fr-FR'` | `lang === 'fr' ? 'fr-FR' : lang === 'es' ? 'es-ES' : 'en-GB'` |

### SessionLogger (Client Component)

SessionLogger is `'use client'` — use `useClientT()`.

- [ ] **Step 2: Add useClientT to SessionLogger**

At the top of `app/client/programme/session/[sessionId]/SessionLogger.tsx`, add:
```typescript
import { useClientT } from '@/components/client/ClientI18nProvider'
```

Inside the component function, add:
```typescript
const { t } = useClientT()
```

Replace hardcoded strings with `t('logger.xyz')` calls. Key replacements:

| Old | New |
|---|---|
| `'Série'` | `t('logger.set')` |
| `'Prévu'` | `t('logger.target.label')` |
| `'Réalisé'` | `t('logger.actual.label')` |
| `'RIR ressenti'` | `t('logger.rir.label')` |
| `'RIR cible'` | `t('logger.rir.target')` |
| `'Gauche'` | `t('logger.side.left')` |
| `'Droite'` | `t('logger.side.right')` |
| `'Repos'` | `t('logger.rest.title')` |
| `'Passer le repos'` | `t('logger.rest.skip')` |
| `'Dépassement'` | `t('logger.rest.overtime')` |
| `'Note de ressenti…'` | `t('logger.note.placeholder')` |
| `'Terminer la séance'` | `t('logger.finish')` |
| `'Terminer ?'` | `t('logger.finish.confirm')` |
| `'sets non complétés'` | `t('logger.finish.incomplete')` |
| `'Terminer quand même'` | `t('logger.finish.action')` |
| `'Continuer'` | `t('logger.finish.cancel')` |
| `'Voir la démo'` | `t('logger.demo.show')` |
| `'Réduire'` | `t('logger.demo.hide')` |
| `'Programme'` section | `t('logger.section')` |
| `'Dernière perf'` | `t('logger.lastPerf')` |

### Recap page (Server Component)

- [ ] **Step 3: Add lang fetch to recap page**

Same pattern — fetch `client_preferences.language` alongside existing data fetches, pass `lang` to `ct()` calls, replace all hardcoded FR strings with `ct(lang, 'recap.xyz')` equivalents.

- [ ] **Step 4: TypeScript check**

```bash
npx tsc --noEmit 2>&1 | grep -E "bilans/page|SessionLogger|recap"
```
Expected: no output.

- [ ] **Step 5: Commit**

```bash
git add app/client/bilans/page.tsx 'app/client/programme/session/[sessionId]/SessionLogger.tsx' 'app/client/programme/recap/[sessionLogId]/page.tsx'
git commit -m "feat(i18n): translate bilans page, SessionLogger, recap page"
```

---

## Task 8: Translate Progress page + Profil page + LogoutButton

**Files:**
- Modify: `app/client/progress/ProgressClientPage.tsx`
- Modify: `app/client/progress/ProgressHeatmap.tsx`
- Modify: `app/client/progress/PRsPodium.tsx`
- Modify: `app/client/progress/page.tsx`
- Modify: `app/client/profil/page.tsx`
- Modify: `app/client/profil/LogoutButton.tsx`

### Progress page

`ProgressClientPage.tsx` is `'use client'`. Add `useClientT()`.

- [ ] **Step 1: Update ProgressClientPage.tsx**

Add import:
```typescript
import { useClientT } from '@/components/client/ClientI18nProvider'
```

Inside component:
```typescript
const { t, tp, ta } = useClientT()
```

Replace:
- `PERIODS` array → use `t('progress.period.7')` etc. in the label fields
- `'Séances'`, `'Volume'`, `'Sets'`, `'Durée moy.'` → `t('progress.kpi.sessions')` etc.
- `'Historique séances'` → `t('progress.history.title')`
- `'sets'` in set count → `t('progress.history.setsLabel')`
- `'Aucune séance sur cette période'` → `t('progress.history.noSessions')`
- `'7j'`, `'30j'` etc. period labels → `t('progress.period.7')` etc.
- `buildInsight` function: translate its output strings using `t()` calls. The function receives `lang` as a parameter and uses `ct()` internally — pass `t` reference or `lang` to it.

`ProgressHeatmap.tsx` is `'use client'`:
- Add `useClientT()`, replace month labels array and day labels array with `ta('progress.heatmap.months')` and `ta('progress.heatmap.days')`.

`PRsPodium.tsx` is `'use client'`:
- Add `useClientT()`, replace `'Records personnels'` with `t('progress.prs.title')`.

`app/client/progress/page.tsx` (Server Component):
- Add lang fetch from `client_preferences`, pass as prop to `ProgressClientPage` if needed for server-rendered strings. For purely client-side strings, `useClientT()` in the child components suffices.

### Profil page

`app/client/profil/page.tsx` is a Server Component.

- [ ] **Step 2: Update profil/page.tsx**

Add imports:
```typescript
import { ct, type ClientLang } from '@/lib/i18n/clientTranslations'
```

Add lang fetch alongside existing `Promise.all` (prefs already fetched, use `preferences.language`):
```typescript
const lang: ClientLang = (['fr','en','es'].includes(preferences.language) ? preferences.language : 'fr') as ClientLang
```

Replace in JSX:
| Old | New |
|---|---|
| `'Compte'` section label | `ct(lang, 'profil.section')` |
| `'Mon profil'` title | `ct(lang, 'profil.title')` |
| `'Actif'` status | `ct(lang, 'profil.status.active')` |
| `'Informations personnelles'` | `ct(lang, 'profil.section.info')` |
| `'Notifications'` | `ct(lang, 'profil.section.notif')` |
| `'Préférences d\'affichage'` | `ct(lang, 'profil.section.prefs')` |
| `'Sécurité'` | `ct(lang, 'profil.section.security')` |
| `'Membre depuis'` | `ct(lang, 'profil.memberSince')` |
| date locale `'fr-FR'` | `lang === 'fr' ? 'fr-FR' : lang === 'es' ? 'es-ES' : 'en-GB'` |

### LogoutButton

`LogoutButton.tsx` is `'use client'`:

- [ ] **Step 3: Update LogoutButton**

```typescript
// app/client/profil/LogoutButton.tsx
'use client'

import { useState } from 'react'
import { LogOut, Loader2 } from 'lucide-react'
import { useRouter } from 'next/navigation'
import { createClient } from '@/utils/supabase/client'
import { useClientT } from '@/components/client/ClientI18nProvider'

export default function ClientLogoutButton() {
  const router = useRouter()
  const { t } = useClientT()
  const [showConfirm, setShowConfirm] = useState(false)
  const [loading, setLoading] = useState(false)

  async function handleLogout() {
    setLoading(true)
    const supabase = createClient()
    await supabase.auth.signOut()
    router.push('/client/login')
  }

  return (
    <>
      <button
        onClick={() => setShowConfirm(true)}
        className="flex items-center justify-center gap-2 w-full h-11 rounded-xl bg-white/[0.02] border-[0.3px] border-white/[0.06] text-[13px] font-medium text-red-400 hover:bg-red-500/[0.06] hover:text-red-400 transition-colors"
      >
        <LogOut size={15} />
        {t('profil.logout')}
      </button>

      {showConfirm && (
        <div className="fixed inset-0 bg-black/50 backdrop-blur-sm z-50 flex items-center justify-center p-4">
          <div className="bg-[#181818] rounded-2xl border-[0.3px] border-white/[0.06] p-6 w-full max-w-sm">
            <h3 className="font-bold text-white mb-2">{t('profil.logout.confirm.title')}</h3>
            <p className="text-[13px] text-white/55 mb-5">{t('profil.logout.confirm.desc')}</p>
            <div className="flex gap-3">
              <button
                onClick={() => setShowConfirm(false)}
                className="flex-1 py-2.5 rounded-xl bg-white/[0.04] text-[13px] text-white/55 hover:text-white/80 transition-colors font-medium"
              >
                {t('profil.logout.cancel')}
              </button>
              <button
                onClick={handleLogout}
                disabled={loading}
                className="flex-1 py-2.5 rounded-xl bg-red-500/80 text-white text-[13px] font-bold hover:bg-red-500 disabled:opacity-50 transition-colors flex items-center justify-center gap-2"
              >
                {loading && <Loader2 size={14} className="animate-spin" />}
                {loading ? t('profil.logout.loading') : t('profil.logout.action')}
              </button>
            </div>
          </div>
        </div>
      )}
    </>
  )
}
```

- [ ] **Step 4: TypeScript check**

```bash
npx tsc --noEmit 2>&1 | grep -E "progress|profil|LogoutButton"
```
Expected: no output.

- [ ] **Step 5: Commit**

```bash
git add app/client/progress/ProgressClientPage.tsx app/client/progress/ProgressHeatmap.tsx app/client/progress/PRsPodium.tsx app/client/progress/page.tsx app/client/profil/page.tsx app/client/profil/LogoutButton.tsx
git commit -m "feat(i18n): translate progress pages and profil page + logout modal"
```

---

## Task 9: Update CHANGELOG + project-state

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `.claude/rules/project-state.md`

- [ ] **Step 1: Update CHANGELOG.md**

Add to the `## 2026-04-16` section at top:
```
FEATURE: Client app i18n — FR/EN/ES full translation via clientTranslations.ts + ClientI18nProvider + useClientT() hook
FEATURE: client_preferences.language now drives live language switch across all client pages on save + reload
```

- [ ] **Step 2: Update project-state.md**

Add a new dated section:
```markdown
## 2026-04-16 — i18n client app complet (FR/EN/ES)

**Ce qui a été fait :**

1. **`lib/i18n/clientTranslations.ts`** — dictionnaire centralisé
   - 3 fonctions : `ct(lang, key, vars?)`, `ctp(lang, key, n)` (pluriel), `cta(lang, key)` (array)
   - Couverture complète : BottomNav, Home, Programme, SessionLogger, Recap, Bilans, Progress, Profil

2. **`components/client/ClientI18nProvider.tsx`** — provider React
   - Charge depuis localStorage (instant, 0 flash) puis depuis `/api/client/preferences` (authoritative)
   - Expose `useClientT()` → `{ lang, t, tp, ta }`

3. **`app/client/layout.tsx`** — wrappé par `<ClientI18nProvider>`

4. **Pages Server Components** (Home, Programme, Bilans, Recap, Profil) : fetch `client_preferences.language` + appel `ct()` direct

5. **Composants Client** (BottomNav, ContextualGreeting, SessionLogger, Logout, Progress, Heatmap, PRsPodium) : `useClientT()`

6. **`PreferencesForm`** — après save, écrit `client_lang` en localStorage + `window.location.reload()` pour appliquer immédiatement

**Points de vigilance :**
- Pages Server Component fetchent la langue à chaque requête — pas de cache stale
- Composants client ont un flash potentiel de 'fr' → langue réelle au premier rendu, atténué par localStorage
- `cta()` pour les arrays (jours, mois) — ne pas utiliser `ct()` sur ces clés
```

- [ ] **Step 3: Commit**

```bash
git add CHANGELOG.md .claude/rules/project-state.md
git commit -m "docs: update CHANGELOG and project-state for client i18n feature"
```

---

## Self-Review

**1. Spec coverage:**
- ✅ FR/EN/ES pour toutes les pages client
- ✅ La langue sélectionnée en préférences s'applique à tout l'app
- ✅ Persistance DB + localStorage
- ✅ Reload immédiat après changement

**2. Placeholder scan:** Aucun TBD, aucun "Add error handling", tout le code est complet.

**3. Type consistency:**
- `ClientLang = 'fr' | 'en' | 'es'` — utilisé partout
- `ClientDictKey` — inféré de `typeof clientDict` — toujours cohérent
- `ct()`, `ctp()`, `cta()` — signatures identiques dans tous les tasks
- `useClientT()` — export nommé dans `ClientI18nProvider.tsx`, importé de ce même fichier partout
